server:
  port: 8081

spring:
  application:
    name: niyamitra-profile-service

  datasource:
    url: jdbc:postgresql://localhost:5432/niyamitra?currentSchema=niyamitra_profiles
    username: niyamitra
    password: niyamitra_dev
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        default_schema: niyamitra_profiles
        dialect: org.hibernate.dialect.PostgreSQLDialect
    open-in-view: false

  flyway:
    enabled: true
    schemas: niyamitra_profiles
    locations: classpath:db/migration

  kafka:
    bootstrap-servers: localhost:9092
    producer:
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.springframework.kafka.support.serializer.JsonSerializer

logging:
  level:
    com.niyamitra: DEBUG
